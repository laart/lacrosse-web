<%= render 'refinery/admin/search_header', :url => refinery.achievements_admin_achievements_path %>
<div class='pagination_container'>
  <% if @achievements.any? %>
    <%= render 'achievements' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
