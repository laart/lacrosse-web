<%= render 'refinery/admin/search_header', :url => refinery.events_admin_repeating_events_path %>
<div class='pagination_container'>
  <% if @repeating_events.any? %>
    <%= render 'repeating_events' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
