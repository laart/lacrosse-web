<% content_for :body do %>
  <ul id="repeating_events">
    <% @repeating_events.each do |repeating_event| %>
      <li>
        <%= link_to repeating_event.repeating_event_title, refinery.events_repeating_event_path(repeating_event) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
