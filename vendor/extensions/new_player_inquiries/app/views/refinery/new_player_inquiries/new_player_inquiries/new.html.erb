<% content_for :body do %>
  <%=raw @page.content_for(Refinery::Pages.default_parts.first.to_sym) %>
<% end %>

<div class="main" role="main">
  <div id="content" class="content full">
    <div class="container">

      <%= render '/refinery/content_page' %>
      <hr>

        <div class="row">

          <div class="col-md-8">

            <header class="single-post-header clearfix">
              <h2 class="post-title"><%= t('.new_player_form_title') %></h2>
            </header>

            <div class="post-content">

              <div class="row">

               <%= form_for [refinery, :new_player_inquiries, @new_player_inquiry], :html => {:id => 'new-player-form'} do |f| %>
                <%= render '/refinery/admin/error_messages',
                           :object => @new_player_inquiry,
                           :include_object_name => true  %>

                  <div class="col-md-12 margin-15">

                    <div class="row">
                      <div class="col-md-6">
                        <%= f.label t('.name') %>
                        <%= f.text_field :name, class: "form-control", placeholder: t('.name') %>
                      </div>

                      <div class="col-md-6">
                        <%= f.label t('.surname') %>
                        <%= f.text_field :surname, class: "form-control", placeholder: t('.surname') %>
                      </div>
                    </div>

                    <div class="row">
                      <div class="col-md-2">
                        <%= f.label t('.age') %>
                        <%= f.number_field :age, class: "form-control input-align-center" %>
                      </div>

                      <div class="col-md-4">
                        <%= f.label t('.phone_number') %>
                        <%= f.phone_field :phone, class: "form-control", placeholder: t('.phone_number') %>
                      </div>

                      <div class="col-md-6">
                        <%= f.label t('.email') %>
                        <%= f.email_field :email, class: "form-control", placeholder: t('.email') %>
                      </div>
                    </div>

                    <div class="form-group">
                      <%= f.label t('.experience_in_sports') %>
                      <%= f.text_area :experience, :cols => 6, :rows => 8, class: "form-control", placeholder: t('.experience_placeholder')  %>
                    </div>

                    <div class="col-md-12">
                      <%= f.submit t('.send'), class: "btn btn-primary btn-lg pull-right" %>
                    </div>

                  </div>

              <% end %>  
              </div>
            </div>
        </div>

        <div class="col-md-4 sidebar">

          <header class="single-post-header clearfix">
            <div class="widget sidebar-widget">
              <div class="sidebar-widget-title">
                <h3><%= t('.our_team') %></h3>
              </div>

              <div class="format-link">
                <%= link_to image_tag("team.jpg"), refinery.players_players_path, class: "media-box" %>
              </div>

            </div>
          </header>

        </div>

      </div>
    </div>
  </div>
</div>


